{
    "cmd": ["cc -w -o '${file_path}/${file_base_name}' *.c"],
    "working_dir": "${file_path}",
    "selector": "source.c",
    "shell": true,
    "variants": [
        {
            "name": "Run",
            "cmd": ["cc -w -o '${file_path}/${file_base_name}' *.c && '${file_path}/${file_base_name}'"]
        }
    ]
}
